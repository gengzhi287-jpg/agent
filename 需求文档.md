# 素材采集 Agent · 需求文档

> 版本：v2.0（已实现）｜ 日期：2026-08-28 ｜ 状态：已完成

## 1. 项目概述
一个本地运行的**素材采集智能体（Agent）**：用户用一句话描述想收集什么素材，大模型作为决策者，自主决定关键词、数量、搜索源，调用工具从网上爬取图片，经**内容校验**后按关键词分类保存到本地 `素材库`。支持**跨会话长期记忆**与**语义检索**。

## 2. 目标与成功标准
- 输入：一句自然语言需求（如「收集 15 张山水风景和 8 张猫咪的图片，尽量高清」）。
- 输出：图片按关键词保存到 `素材库/关键词/图片`，无重复、无重名覆盖、内容与主题相符。
- 成功标准：Agent 自主完成采集并总结；保存图片与关键词匹配；进度实时反馈；记忆跨会话累积。

## 3. 功能需求（均已实现）

### 3.1 Agent 编排
- 大模型（支持 function calling）作为编排者，循环调用工具完成任务。
- 工具：
  - `collect_keyword`（按关键词采集，可带数量/搜索源/尺寸/主题过滤）
  - `collect_url`（抓指定网页 `<img>`）
  - `list_saved`（查看素材库结构）
- 模型根据工具返回 JSON 自主调整策略（换关键词、换搜索源、调整数量），最后用中文总结。
- 自主性上限：单次任务最多 15 轮工具调用（`max_iter=15`）。

### 3.2 内容校验（命中率）
- 下载每张图后，让多模态模型识别主题，并**判断是否与关键词相关**。
- 不相关的图直接丢弃，计入「与主题不符」，避免「搜零食存成鸟类」。
- 模型判断失败时默认放行，避免误删正确图片。

### 3.3 采集与搜索源
- 搜索源可切换：Bing（免 Key）/ Pixabay / Pexels（需 Key）；未配 Key 自动回退 Bing。
- 支持网页 URL 提取图片。

### 3.4 保存与命名
- 目录结构：**单级** `素材库/关键词/图片`（关键词内不再细分主题子目录）。
- 命名：`序号_关键词.ext`（如 `01_手机.jpg`）；MD5 内容去重；重名自动加序号。
- 仅保存 ≤1MB 的 `.jpg/.jpeg/.png`，不含动图。

### 3.5 过滤规则
- `min_width` / `min_height`：小于指定尺寸跳过。
- `exclude_topics`：命中的主题不保存。
- 过滤 / 校验失败均计入失败数并在报告中体现原因。

### 3.6 跨会话记忆（语义 RAG）
- 记忆存储：长期规则 `memory.json` + 近期历史 `logs/history.json` + 向量缓存 `memory_vectors.json`。
- 检索：用 embedding 模型把用户目标与记忆向量化，按**语义相似度**只取最相关的 top-K 条注入系统提示。
- 去重：新规则与已有规则语义余弦 ≥0.72 或重叠系数 ≥0.75 时拒收。
- 遗忘：规则记录使用次数与最近使用时间，每天自动清理「90 天没用 + 用 <2 次」的冷门规则。
- 降级：embedding 不可用时自动回退本地 TF-IDF 检索。

## 4. 界面需求（本地网页）
- 🤖 Agent 输入框：一句话下达需求。
- 实时进度：各关键词下载/保存/失败数。
- 暂停 / 继续 / 取消（取消有二次确认）。
- 任务完成提示音。
- 结果汇总报告（可导出 txt）。
- 任务历史（持久化，可回查详情）。
- 素材浏览（缩略图网格，按关键词/主题筛选）。
- 记忆管理面板（查看/添加/删除长期规则）。
- 设置模型弹窗（预设切换 + Base URL / API Key / 模型名）。

## 5. 外部依赖与配置
- 模型：OpenAI 兼容多模态接口；当前为火山引擎 **Agent Plan 套餐**：
  - `base_url = https://ark.cn-beijing.volces.com/api/plan/v3`（套餐接口，勿用 `/api/v3`）
  - `model = doubao-seed-2-1-turbo-260628`
  - `embedding_model = doubao-embedding-vision-251215`
- 支持 `llm.presets` 预设多套模型；预设 key 由服务端读取、不下发到页面。
- 搜索源：Bing / Pixabay / Pexels。
- 日志：`logs/时间戳.log`；历史：`logs/history.json`；记忆：`memory.json` / `memory_vectors.json`。

## 6. 非功能需求
- 健壮性：单张失败不影响整体，失败计入报告；接口错误（401/403/404/429）有友好提示。
- 可观测性：每步日志落盘。
- 去重基于文件内容（MD5）。
- 依赖：`flask` / `requests` / `pyyaml` / `pillow`（Python 3.9+）。

## 7. 交付物
- 后端：`app.py` / `agent.py` / `crawler.py` / `classifier.py` / `saver.py` / `memory.py` / `logger_setup.py`
- 前端：`templates/index.html`
- 配置：`config.yaml` / `config.example.yaml`
- 文档：`README.md`（使用说明）/ 本需求文档
- 数据：`素材库/`（运行时生成）、`logs/`、`memory.json`、`memory_vectors.json`

## 8. 接口速览
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/agent/start` | 启动 Agent 任务（body: `{goal, preset?}`） |
| GET | `/api/status` | 运行状态与进度 |
| POST | `/api/pause` `/api/resume` `/api/cancel` | 暂停 / 继续 / 取消 |
| GET | `/api/report` `/api/export` | 结果汇总 / 导出 txt |
| GET | `/api/history` `/api/history/<id>` | 任务历史 / 详情 |
| GET/POST/DELETE | `/api/memory` | 记忆查看 / 新增 / 删除规则 |
| GET | `/api/browse` | 素材库树 |
| GET | `/media/<path>` | 图片文件 |
| GET | `/api/config` | 配置（模型/预设/搜索源） |

## 9. 变更记录
- **v2.0（2026-08-28）**：
  - 新增**跨会话记忆**：语义 RAG（embedding 检索）+ 自动去重 + 自动遗忘 + 向量缓存 + TF-IDF 降级。
  - 新增**内容校验**：下载后判断图片是否与关键词相关，不相关丢弃（提升命中率）。
  - 移除手动模式，全面改为 **Agent-only**（`/api/start` 删除，改用 `/api/agent/start`）。
  - 分类改为**单级目录**（去掉关键词内的主题子目录与「未分类」次级文件夹）。
  - 记忆 API `/api/memory`、记忆管理面板、日志去掉「主题=未分类」字样。
  - 配置文件更新为火山套餐接口与模型。
- **v1.2（2026-08-27）**：
  - 新增 🤖 Agent 模式（大模型工具调用编排自主采集）。
  - 新增暂停/继续/取消、完成提示音、结果汇总报告、任务历史、素材浏览。
  - 搜索源增强（Pixabay / Pexels）；过滤规则；模型配置改为通用 OpenAI 兼容并支持预设。
